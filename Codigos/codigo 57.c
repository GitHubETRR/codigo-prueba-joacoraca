#include <stdio.h>

void mayorDeDos(int a, int b) {
    if (a > b) {
        printf("El mayor es %d\n", a);
    } else {
        printf("El mayor es %d\n", b);
    }
}

int getMayorDeDos(int a, int b) {
    if (a > b) {
        return a;
    } else {
        return b;
    }
}
int main() {
    void mayorDeDos(int a, int b) {
    if (a > b) {
        printf("El mayor es: %d\n", a);
    } else {
        printf("El mayor es: %d\n", b);
    }
    }
}
